# Reference Algorithms

These scripts are minimal, inspectable seeds. They are not a production parser or model.

- `build_h5_seed.py`: create the HDF5 seed store from JSON files.
- `compute_metrics.py`: reference functions for mass, phase constraints and consensus.
